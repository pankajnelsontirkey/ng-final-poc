import { PathStringFilterPipe } from './path-string-filter.pipe';

describe('PathStringFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PathStringFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
